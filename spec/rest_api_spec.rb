RSpec.describe RESTAPI do
  it "has a version number" do
    expect(RackEnstein::VERSION).not_to be nil
  end
  describe Component do
    it "get the create the right device" do
      expect(false).to eq(true)
    end
  end
end
